version: "3.9"

services:
  frontend:
    build: .
    container_name: fln-frontend
    ports:
      - "80"
    restart: unless-stopped
    mem_limit: 64m
    netwoeks:
      - fln-net

networks:
  fln-net:
    external: true
